node_modules/
dist/
*.tsbuildinfo
coverage/
.DS_Store

# Example private keys are regenerated by `pnpm --filter @peaceos/examples run generate`.
# Only their public halves and the resulting packages are committed.
examples/keys/*.key

# Generated from the transparency/registry submodule by
# web/scripts/build-embedded-registry.mjs on every dev/build/test run.
web/src/embeddedRegistry.generated.ts

# Rust build output and Tauri's generated capability schemas/permissions.
desktop/src-tauri/target/
desktop/src-tauri/gen/
desktop/src-tauri/permissions/autogenerated/

# CI-only browser-automation tooling (desktop/ci/) — not a workspace member,
# installed fresh by the CI job that uses it.
desktop/ci/package-lock.json
