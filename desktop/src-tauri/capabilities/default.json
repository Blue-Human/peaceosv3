{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Phase A: only the core window/webview permissions needed to display the portal. No fs, http, or shell access — there is no capability that could reach the network or the filesystem from the frontend.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
