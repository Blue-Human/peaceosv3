{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window/webview permissions, plus our own two app-defined commands (registry_update.rs) — no fs, http, or shell plugin, so there is no general-purpose way to reach the network or filesystem from the frontend beyond those two narrow, hard-coded commands.",
  "windows": ["main"],
  "permissions": ["core:default", "allow-update-organizations-registry", "allow-load-local-registry-state"]
}
